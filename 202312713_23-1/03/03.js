const answer = `
Node.js에서 모듈을 불러올 때, require() 함수를 사용합니다. 이 require() 함수는 다음 두 가지 방식으로 모듈을 불러올 수 있습니다.

1. 내장 모듈 (Core Modules) 불러오기
내장 모듈은 Node.js에 기본적으로 내장된 모듈로, 파일 시스템, 네트워킹 등의 기능을 제공합니다. 이러한 내장 모듈은 require() 함수를 호출할 때 모듈 이름만 지정하면 됩니다. 예를 들어, http 모듈을 불러오는 방법은 다음과 같습니다.

const http = require('http');


2. 사용자 정의 모듈 불러오기
사용자 정의 모듈은 프로그래머가 직접 작성한 모듈입니다. 이러한 사용자 정의 모듈은 require() 함수를 호출할 때 모듈 파일의 경로를 지정해야 합니다. 예를 들어, 사용자 정의 모듈인 myModule.js를 불러오는 방법은 다음과 같습니다.

const myModule = require('./myModule.js');

위의 코드에서 require() 함수의 인수로는 모듈 파일의 경로를 전달합니다. 경로를 지정할 때는 ./를 붙여서 현재 디렉토리를 기준으로 상대 경로를 지정하거나, 절대 경로를 지정할 수 있습니다.

이러한 두 가지 방식을 통해 Node.js에서 모듈을 불러올 수 있습니다.
`;
console.log(answer);